#quiet_cmd_run_linenoise  = SH      $(patsubst $(srctree)/%,%,$<)
#      cmd_run_linenoise  = /bin/bash $(srctree)/scripts/mkulinenoise.sh $(srctree)

#obj-y := linenoise/linenoise_lib.o

#$(obj)/linenoise/linenoise_lib.o: $(srctree)/scripts/mkulinenoise.sh
#	$(call if_changed,run_linenoise)

